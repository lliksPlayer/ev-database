# LLM Wiki Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Set up a complete, operational LLM-maintained knowledge base inside the existing Obsidian vault at `Brain/`, ready for first ingest.

**Architecture:** Flat directory layout under `Brain/` with LLM-owned `wiki/`, user-owned `raw/`, and a `CLAUDE.md` schema that governs all wiki behavior across sessions. No code, no tooling — pure markdown files and a comprehensive instruction document.

**Tech Stack:** Markdown, YAML frontmatter, Obsidian, git

---

### Task 1: Create directory scaffold

**Files:**
- Create: `Brain/raw/.gitkeep`
- Create: `Brain/raw/assets/.gitkeep`
- Create: `Brain/wiki/projects/.gitkeep`
- Create: `Brain/wiki/decisions/.gitkeep`
- Create: `Brain/wiki/concepts/.gitkeep`
- Create: `Brain/wiki/entities/.gitkeep`
- Create: `Brain/wiki/sources/.gitkeep`

- [ ] **Step 1: Create all directories with placeholder files**

```bash
mkdir -p "Brain/raw/assets"
mkdir -p "Brain/wiki/projects"
mkdir -p "Brain/wiki/decisions"
mkdir -p "Brain/wiki/concepts"
mkdir -p "Brain/wiki/entities"
mkdir -p "Brain/wiki/sources"
touch "Brain/raw/.gitkeep"
touch "Brain/raw/assets/.gitkeep"
touch "Brain/wiki/projects/.gitkeep"
touch "Brain/wiki/decisions/.gitkeep"
touch "Brain/wiki/concepts/.gitkeep"
touch "Brain/wiki/entities/.gitkeep"
touch "Brain/wiki/sources/.gitkeep"
```

- [ ] **Step 2: Verify structure**

```bash
find Brain -type f | sort
```

Expected output includes all 7 `.gitkeep` files plus `Brain/CLAUDE.md` and `Brain/Willkommen.md`.

- [ ] **Step 3: Commit**

```bash
git add Brain/raw Brain/wiki
git commit -m "feat: wiki directory scaffold"
```

---

### Task 2: Create `log.md`

**Files:**
- Create: `Brain/log.md`

- [ ] **Step 1: Write `Brain/log.md`**

```markdown
# Log

Append-only operation history. Each entry starts with `## [YYYY-MM-DD] operation | title`.

Parse last 5 entries: `grep "^## \[" log.md | tail -5`

---

## [2026-04-17] init | Wiki initialized
```

- [ ] **Step 2: Commit**

```bash
git add Brain/log.md
git commit -m "feat: add wiki log.md"
```

---

### Task 3: Create `index.md`

**Files:**
- Create: `Brain/index.md`

- [ ] **Step 1: Write `Brain/index.md`**

```markdown
# Wiki Index

Master index of all wiki pages. Updated by the LLM on every ingest, query (if a new page is filed), or lint pass.

---

## Overview

| Page | Summary |
|------|---------|
| [[wiki/overview]] | High-level synthesis of the entire wiki |

## Projects

| Page | Summary | Sources |
|------|---------|---------|
| *(none yet)* | | |

## Decisions

| Page | Summary | Date |
|------|---------|------|
| *(none yet)* | | |

## Concepts

| Page | Summary |
|------|---------|
| *(none yet)* | |

## Entities

| Page | Summary |
|------|---------|
| *(none yet)* | |

## Sources

| Page | Original file | Date |
|------|--------------|------|
| *(none yet)* | | |
```

- [ ] **Step 2: Commit**

```bash
git add Brain/index.md
git commit -m "feat: add wiki index.md"
```

---

### Task 4: Create `wiki/overview.md`

**Files:**
- Create: `Brain/wiki/overview.md`

- [ ] **Step 1: Write `Brain/wiki/overview.md`**

```markdown
---
type: overview
tags: []
last_updated: 2026-04-17
---

*EN: High-level synthesis of all knowledge in this wiki. Updated when ingests change the big picture.*

# Übersicht

Dieses Wiki wird vom LLM gepflegt und dokumentiert Softwareentwicklungswissen, beginnend mit dem Projekt **ev-vergleich**.

## Projekte

*(Noch keine Projekte dokumentiert — beim ersten Ingest wird diese Seite aktualisiert.)*

## Offene Fragen

*(Werden beim Ingest gesammelt.)*

## Letzte Änderung

2026-04-17 — Wiki initialisiert.
```

- [ ] **Step 2: Commit**

```bash
git add Brain/wiki/overview.md
git commit -m "feat: add wiki/overview.md stub"
```

---

### Task 5: Write `CLAUDE.md` — the wiki schema

This is the most important file. It governs all LLM behavior in this vault across every session.

**Files:**
- Modify: `Brain/CLAUDE.md`

- [ ] **Step 1: Write `Brain/CLAUDE.md`**

```markdown
# Wiki-Agent — Schema & Anweisungen

Du bist der Wiki-Agent für diese Wissensdatenbank. Du pflegst den gesamten Inhalt in `wiki/`, `index.md` und `log.md`. Du liest `raw/`, schreibst aber **nie** darin.

---

## Eigentumsregeln

| Pfad | Eigentümer | Regel |
|------|-----------|-------|
| `raw/` | Nutzer | LLM liest nur, nie schreiben |
| `wiki/` | LLM | LLM erstellt und pflegt alle Seiten |
| `index.md` | LLM | Nach jedem Ingest aktualisieren |
| `log.md` | LLM | Nur anhängen, nie löschen |
| `CLAUDE.md` | Gemeinsam | Entwickelt sich durch Gespräche weiter |

---

## Verzeichnisstruktur

```
Brain/
├── CLAUDE.md              # Diese Datei
├── index.md               # Master-Seitenindex
├── log.md                 # Append-only Verlauf
├── raw/                   # Unveränderliche Quellen (Nutzer-Eigentum)
│   └── assets/            # Heruntergeladene Bilder
└── wiki/                  # Alle LLM-generierten Seiten
    ├── overview.md        # Gesamtsynthese
    ├── projects/          # Eine Seite pro Projekt
    ├── decisions/         # Architekturentscheidungen (ADR-Stil)
    ├── concepts/          # Technische Konzepte und Muster
    ├── entities/          # Personen, Tools, Libraries, Services
    └── sources/           # Eine Zusammenfassung pro Quelle
```

---

## Operation: Ingest

**Auslöser:** Nutzer schreibt `"ingest: <dateiname oder beschreibung>"` oder legt eine Datei in `raw/` ab.

**Schritte (in dieser Reihenfolge):**

1. Quelldatei lesen
2. 2–4 wichtige Erkenntnisse kurz mit dem Nutzer besprechen
3. `wiki/sources/<slug>.md` Zusammenfassungsseite schreiben
4. Alle berührten Seiten in `wiki/projects/`, `wiki/decisions/`, `wiki/concepts/`, `wiki/entities/` aktualisieren — fehlende Seiten neu anlegen
5. `wiki/overview.md` aktualisieren, wenn die Quelle das Gesamtbild verändert
6. `index.md` mit neuen oder geänderten Seiten aktualisieren
7. Eintrag an `log.md` anhängen: `## [YYYY-MM-DD] ingest | <titel>`

Ein einzelner Ingest berührt typischerweise 5–15 Wiki-Seiten.

---

## Operation: Query

**Auslöser:** Nutzer stellt eine Frage natürlich, oder schreibt `"frage: <frage>"`.

**Schritte:**

1. `index.md` lesen, um relevante Seiten zu finden
2. Diese Seiten lesen
3. Antwort mit expliziten Wiki-Zitaten (`[[seitenname]]`) formulieren
4. Wenn die Antwort substanziell und wiederverwendbar ist → als neue Wiki-Seite ablegen und `index.md` aktualisieren

---

## Operation: Lint

**Auslöser:** Nutzer schreibt `"lint"`.

**Prüfungen:**
- Widersprüche zwischen Seiten
- Veraltete Aussagen, die neuere Quellen überholt haben
- Waisenseiten (keine eingehenden Links)
- Konzepte erwähnt, aber ohne eigene Seite
- Fehlende Querverweise
- Datenlücken, die mit einer Websuche gefüllt werden könnten

**Ausgabe:** Kurze Aufgabenliste im Chat. Keine automatischen Änderungen — der Nutzer entscheidet, was zu tun ist.

---

## Seitenformate

### Frontmatter (alle Wiki-Seiten)

```yaml
---
type: source | decision | concept | entity | project | overview
tags: []
last_updated: YYYY-MM-DD
source_count: N        # nur bei entity/concept/project-Seiten
---
```

### Sprachregeln

- Jede Seite in der dominanten Sprache ihrer Quelle(n) schreiben
- Eine einzeilige Zusammenfassung in der jeweils anderen Sprache direkt unter dem Frontmatter hinzufügen
- Dies ermöglicht sprachübergreifende Auffindbarkeit in Obsidian

### Quellenübersicht (`wiki/sources/<slug>.md`)

```markdown
---
type: source
tags: []
last_updated: YYYY-MM-DD
---

*EN: One-line English summary*

# Titel der Quelle

**Datum:** YYYY-MM-DD  
**Originaldatei:** `raw/<dateiname>`

## Zusammenfassung

- Punkt 1
- Punkt 2
- Punkt 3

## Verweise

- [[wiki/concepts/<slug>]] — ...
- [[wiki/decisions/<slug>]] — ...
```

### Entscheidung (`wiki/decisions/<slug>.md`)

```markdown
---
type: decision
tags: []
last_updated: YYYY-MM-DD
---

*EN: One-line English summary*

# Entscheidungstitel

**Datum:** YYYY-MM-DD  
**Status:** entschieden | überholt | offen

## Kontext

Warum war diese Entscheidung notwendig?

## Entscheidung

Was wurde entschieden?

## Alternativen

- Alternative A — warum verworfen
- Alternative B — warum verworfen

## Konsequenzen

Was ändert sich dadurch?
```

### Konzept (`wiki/concepts/<slug>.md`)

```markdown
---
type: concept
tags: []
last_updated: YYYY-MM-DD
source_count: N
---

*EN: One-line English summary*

# Konzeptname

## Definition

Was ist dieses Konzept?

## Verwendung im Projekt

Wie wird es hier eingesetzt?

## Verwandte Konzepte

- [[wiki/concepts/<anderes-konzept>]]
```

### Entität (`wiki/entities/<slug>.md`)

```markdown
---
type: entity
tags: []
last_updated: YYYY-MM-DD
source_count: N
---

*EN: One-line English summary*

# Entitätsname

## Was ist das?

Kurze Beschreibung.

## Rolle im Projekt

Wie wird es verwendet?

## Relevante Links

- [[wiki/sources/<slug>]]
```

### Projekt (`wiki/projects/<slug>.md`)

```markdown
---
type: project
tags: []
last_updated: YYYY-MM-DD
source_count: N
---

*EN: One-line English summary*

# Projektname

**Status:** aktiv | pausiert | abgeschlossen

## Ziele

Was soll dieses Projekt erreichen?

## Wichtige Entscheidungen

- [[wiki/decisions/<slug>]]

## Offene Fragen

- Frage 1

## Quellen

N Quellen indexiert.
```

---

## Log-Format

```
## [YYYY-MM-DD] ingest | Titel
## [YYYY-MM-DD] query | Gestellte Frage
## [YYYY-MM-DD] lint | N Probleme gefunden
```

Letzte 5 Einträge parsen: `grep "^## \[" log.md | tail -5`

---

## Erweiterungspfad

Wenn ein zweites Projekt hinzugefügt wird:
- Neue Seite `wiki/projects/<neues-projekt>.md` anlegen
- Keine Strukturänderungen nötig — das flache `wiki/`-Layout unterstützt mehrere Projekte von Natur aus
- Bei mehr als ~200 Seiten: Such-Tool (z.B. `qmd`) als CLI einbinden
```

- [ ] **Step 2: Verify the file was written correctly**

```bash
wc -l "Brain/CLAUDE.md"
```

Expected: more than 150 lines.

- [ ] **Step 3: Commit**

```bash
git add Brain/CLAUDE.md
git commit -m "feat: write wiki schema in CLAUDE.md"
```

---

### Task 6: Create first project page for ev-vergleich

**Files:**
- Create: `Brain/wiki/projects/ev-vergleich.md`

- [ ] **Step 1: Write `Brain/wiki/projects/ev-vergleich.md`**

```markdown
---
type: project
tags: [ev, firebase, react]
last_updated: 2026-04-17
source_count: 0
---

*EN: EV comparison tool — a web app for comparing electric vehicles, built with React and Firebase.*

# ev-vergleich

**Status:** aktiv

## Ziele

Ein Webvergleichstool für Elektrofahrzeuge, das Nutzern ermöglicht, EV-Modelle anhand von Reichweite, Preis, Ladezeit und TCO (Total Cost of Ownership) zu vergleichen.

## Technologie-Stack

- React (Frontend)
- Firebase / Firestore (Datenbank, Hosting)
- Vercel (Deployment-Alternative)

## Wichtige Entscheidungen

*(Werden beim ersten Ingest ergänzt.)*

## Offene Fragen

*(Werden beim ersten Ingest gesammelt.)*

## Quellen

0 Quellen indexiert.
```

- [ ] **Step 2: Update `index.md` to include the new project page**

In `Brain/index.md`, replace the Projects table row `*(none yet)*` with:

```markdown
| [[wiki/projects/ev-vergleich]] | EV-Vergleichstool mit React und Firebase | 0 |
```

- [ ] **Step 3: Update `log.md`**

Append to `Brain/log.md`:

```markdown

## [2026-04-17] ingest | ev-vergleich Projektseite angelegt
```

- [ ] **Step 4: Commit**

```bash
git add Brain/wiki/projects/ev-vergleich.md Brain/index.md Brain/log.md
git commit -m "feat: add ev-vergleich project page and update index"
```

---

### Task 7: Smoke-test the full ingest workflow

This task validates that the schema works end-to-end by doing a real mini-ingest.

**Files:**
- Create: `Brain/raw/test-ingest.md` (temporary test source)
- Create: `Brain/wiki/sources/test-ingest.md`

- [ ] **Step 1: Create a minimal test source in `raw/`**

```bash
cat > "Brain/raw/test-ingest.md" << 'EOF'
# Test-Quelle

Datum: 2026-04-17

Diese Datei dient als Smoke-Test für den Ingest-Workflow.

## Entscheidung: Firebase als Datenbank

Wir verwenden Firebase Firestore als primäre Datenbank, weil es Real-time-Updates,
eine einfache Authentifizierung und kostenloses Hosting für kleine Projekte bietet.

## Konzept: TCO (Total Cost of Ownership)

TCO umfasst Kaufpreis, Kraftstoff-/Stromkosten, Versicherung und Wartung über
5 Jahre. Basis für alle Fahrzeugvergleiche.
EOF
```

- [ ] **Step 2: Write `Brain/wiki/sources/test-ingest.md`**

```markdown
---
type: source
tags: [test, firebase, tco]
last_updated: 2026-04-17
---

*EN: Smoke-test source — validates ingest workflow with a Firebase decision and TCO concept.*

# Test-Quelle

**Datum:** 2026-04-17  
**Originaldatei:** `raw/test-ingest.md`

## Zusammenfassung

- Firebase Firestore als primäre Datenbank gewählt
- TCO als zentrales Vergleichskonzept definiert
- Smoke-Test für den Ingest-Workflow

## Verweise

- [[wiki/decisions/firebase-als-datenbank]] — Datenbankentscheidung
- [[wiki/concepts/tco]] — Total Cost of Ownership
```

- [ ] **Step 3: Write `Brain/wiki/decisions/firebase-als-datenbank.md`**

```markdown
---
type: decision
tags: [firebase, datenbank, infrastruktur]
last_updated: 2026-04-17
---

*EN: Decision to use Firebase Firestore as the primary database for ev-vergleich.*

# Firebase als Datenbank

**Datum:** 2026-04-17  
**Status:** entschieden

## Kontext

Für ev-vergleich wird eine Datenbank benötigt, die Real-time-Updates unterstützt,
einfach zu authentifizieren ist und für kleine Projekte kostenlos bleibt.

## Entscheidung

Firebase Firestore wird als primäre Datenbank verwendet.

## Alternativen

- Supabase — ähnliche Features, aber weniger Erfahrung im Team
- Lokale JSON-Datei — keine Real-time-Updates möglich

## Konsequenzen

- Firestore-spezifische Query-Syntax muss beherrscht werden
- Offline-Fähigkeit durch Firestore-SDK eingebaut
- Kosten steigen bei sehr hohem Datenvolumen
```

- [ ] **Step 4: Write `Brain/wiki/concepts/tco.md`**

```markdown
---
type: concept
tags: [tco, kosten, vergleich]
last_updated: 2026-04-17
source_count: 1
---

*EN: Total Cost of Ownership — the primary comparison metric across all vehicle entries.*

# TCO (Total Cost of Ownership)

## Definition

TCO bezeichnet die Gesamtkosten eines Fahrzeugs über einen definierten Zeitraum
(Standard: 5 Jahre), bestehend aus:

- Kaufpreis
- Kraftstoff- bzw. Stromkosten
- Versicherung
- Wartung und Reparaturen

## Verwendung im Projekt

Alle Fahrzeugvergleiche in ev-vergleich basieren auf dem TCO-Wert als primäre
Vergleichsmetrik. Ermöglicht faire Gegenüberstellung von EV und Verbrenner.

## Verwandte Konzepte

- [[wiki/projects/ev-vergleich]]
```

- [ ] **Step 5: Update `index.md` — add new pages to Decisions, Concepts, Sources tables**

In `Brain/index.md`, replace `*(none yet)*` in Decisions with:
```markdown
| [[wiki/decisions/firebase-als-datenbank]] | Firebase Firestore als primäre Datenbank | 2026-04-17 |
```

Replace `*(none yet)*` in Concepts with:
```markdown
| [[wiki/concepts/tco]] | Total Cost of Ownership — primäre Vergleichsmetrik |
```

Replace `*(none yet)*` in Sources with:
```markdown
| [[wiki/sources/test-ingest]] | `raw/test-ingest.md` | 2026-04-17 |
```

- [ ] **Step 6: Update `wiki/projects/ev-vergleich.md` — add decision and source count**

Replace the `## Wichtige Entscheidungen` section with:
```markdown
## Wichtige Entscheidungen

- [[wiki/decisions/firebase-als-datenbank]] — Firebase Firestore als primäre Datenbank
```

Update frontmatter `source_count` from `0` to `1`.

- [ ] **Step 7: Update `wiki/overview.md` — reflect first real content**

Replace the `## Projekte` section body with:

```markdown
## Projekte

- [[wiki/projects/ev-vergleich]] — EV-Vergleichstool mit React und Firebase (aktiv)

## Schlüsselentscheidungen

- [[wiki/decisions/firebase-als-datenbank]] — Firebase Firestore als primäre Datenbank

## Zentrale Konzepte

- [[wiki/concepts/tco]] — Total Cost of Ownership als primäre Vergleichsmetrik
```

Update frontmatter `last_updated` to `2026-04-17`.

- [ ] **Step 9: Append to `log.md`**

```markdown

## [2026-04-17] ingest | test-ingest.md — Smoke-Test Ingest
```

- [ ] **Step 10: Commit**

```bash
git add Brain/
git commit -m "feat: smoke-test ingest — firebase decision, tco concept, source summary"
```

- [ ] **Step 11: Verify Obsidian links work**

Open `Brain/index.md` in Obsidian and confirm that `[[wiki/projects/ev-vergleich]]`, `[[wiki/decisions/firebase-als-datenbank]]`, and `[[wiki/concepts/tco]]` resolve to real pages (no red/unresolved links).

---

### Task 8: Clean up test source (optional)

The `raw/test-ingest.md` file was only for the smoke test. Keep it (it's a valid first source) or delete it — user decides.

- [ ] **Step 1: Decide whether to keep or delete `raw/test-ingest.md`**

To keep: no action needed.

To delete:
```bash
rm "Brain/raw/test-ingest.md"
git add Brain/raw/test-ingest.md
git commit -m "chore: remove smoke-test source file"
```

Note: The wiki pages created from it (`wiki/sources/test-ingest.md`, `wiki/decisions/firebase-als-datenbank.md`, `wiki/concepts/tco.md`) are real content and should be kept regardless.
```
